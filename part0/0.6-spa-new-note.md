# 0.6 – Single page app new note diagram

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa {content: 'new note', date: 'now'}
    activate server
    server-->>browser: returns response 201 Created
    deactivate server

    Note right of browser: Browser executes JS and fetches updated notes JSON

    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/data.json
    activate server
    server-->>browser: JSON notes
    deactivate server

    Note right of browser: Browser renders notes
